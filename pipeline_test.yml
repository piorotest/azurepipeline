variables:
   URL_SHELL: $(URL)
   API_KEY_SHELL: $(API_KEY)
   PIPENAME_SHELL: $(Build.BuildNumber)

pool:
  name: Azure Pipelines

resources:
  repositories:
  - repository: templates
    type: git
    name: marcindemo/pipeline_test
    ref: refs/heads/main


extends: 
  template: delphix_bookmarks.yml
  parameters:
    usersteps:
    - task: Bash@3
      inputs:
        targetType: 'inline'
        script: |
          echo "placeholder"
      displayName: Run destrictive test

